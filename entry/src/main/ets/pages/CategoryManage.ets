@Preview
@Entry
@Component
struct CategoryManage {
  @Builder
  build() {

    Column() {
      Text("empty")
    }
    .width("100%")
    .height("100%")
    .backgroundColor($r("app.color.back_color"))

  }
}